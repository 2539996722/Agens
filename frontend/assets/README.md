# assets/

此处预留静态资源目录。当前版本为**零依赖前端**，所有视觉效果都由 CSS / 内联 SVG / Google Fonts 实现，因此本目录暂时为空。

后续可放置的内容：
- `logo.svg` / `favicon.svg` — 自制品牌图标
- `illustrations/` — 封面插画、节日贴纸
- `stickers/` — PNG 贴纸

如需图标，请优先使用 CSS 手绘 SVG（参见 `css/sketch.css` 中已定义的 `scribble-arrow` 样式），保持「手帐风」的视觉一致性。